{
  "name": "Qiuyu Lu personal website (Jekyll / GitHub Pages)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "install": "gem install bundler -v 2.2.19 && bundle _2.2.19_ install",
  "terminals": [
    {
      "name": "jekyll-server",
      "command": "bundle _2.2.19_ exec jekyll liveserve --host 0.0.0.0 --port 4000",
      "description": "Jekyll dev server with live reload. Serves the site on http://localhost:4000 (LiveReload websocket on port 35729)."
    }
  ],
  "ports": [
    { "name": "jekyll", "port": 4000 },
    { "name": "livereload", "port": 35729 }
  ]
}
